function [seq_out] = hd(seq_in,X,label)
%HD Summary of this function goes here
%   Detailed explanation goes here

seq_out = zeros(1,length(seq_in)*size(label,2));
m = size(label,2);
mapped = [];

for i = 1:lenght(seq_in)
    for

